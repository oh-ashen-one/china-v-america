{
  "project": "china-v-america",
  "branchName": "ralph/landing",
  "description": "Cinematic China vs America AI landing. Desktop 1440-1920 is the film hero. YouTube A-roll, not a SaaS template.",
  "userStories": [
    {
      "id": "US-001",
      "title": "Next.js app builds with HTTP-ready homepage",
      "description": "Scaffold Next.js App Router + TS. No Inter. No fake next/font subsets (Noto Serif SC only allows cyrillic/latin/latin-ext/vietnamese). Homepage must exist.",
      "acceptanceCriteria": [
        "package.json has next + react + typescript",
        "src/app/page.tsx and layout.tsx exist",
        "npm install && npm run build exits 0"
      ],
      "priority": 1,
      "passes": true,
      "verify": "npm install && npm run build"
    },
    {
      "id": "US-002",
      "title": "Dual-rail visual system",
      "description": "Persistent desktop dual-rail with a glowing vs-spine. America tokens: --us-ink, --us-nasa, --us-amber, grotesque type, hairline rules. China tokens: --cn-lacquer, --cn-cinnabar, --cn-jade, --cn-gold, --cn-cyan. Cursor X crossfades which rail is lit. Mobile stacks with a US/CN switcher. This must look like a film frame, not two colored columns.",
      "acceptanceCriteria": [
        "globals.css defines --us- and --cn- token sets including amber, nasa/blue, cinnabar, jade, gold",
        "Desktop layout is dual-rail with a center spine",
        "npm run build exits 0"
      ],
      "priority": 2,
      "passes": true,
      "verify": "npm run build && grep -E -q -- '--us-' src/app/globals.css && grep -E -q -- '--cn-' src/app/globals.css && grep -E -q -- '--us-(amber|nasa|blue)' src/app/globals.css && grep -E -q -- '--cn-(cinnabar|jade|gold|cyan|lacquer)' src/app/globals.css && grep -R -E -q 'spine|dual-rail|data-rail|rail-us' src"
    },
    {
      "id": "US-003",
      "title": "Diegetic 8-14s boot, skippable",
      "description": "Not a Bootstrap bar. A diegetic power-on for two civilizations. Skip control. localStorage remembers skip after first visit. Boot is used from the homepage. NEVER emit src/app/globals.css \u2014 on-disk CSS already builds (641 lines, braces balanced). LAST_VERIFY 'Unknown word' at line 948 is stale from a discarded rewrite. Put boot motion in boot.tsx only (inline styles or a style tag).",
      "acceptanceCriteria": [
        "Boot component exists and is used from the homepage",
        "Skip path exists",
        "npm run build exits 0"
      ],
      "priority": 3,
      "passes": true,
      "verify": "python3 scripts/ralph/strip_markers.py && npm run build && grep -q 'Boot' src/app/page.tsx && grep -q 'localStorage' src/components/boot.tsx && grep -q -E 'skip|Skip' src/components/boot.tsx"
    },
    {
      "id": "US-004",
      "title": "Hero thesis + CTA",
      "description": "One sharp sentence about the compute race. CTA label exactly: Enter the comparison. Hero sits inside the dual-rail, not a centered SaaS slab. NEVER emit src/app/globals.css \u2014 it already builds. Edit page.tsx (and a small hero component if needed). Do not restart the rails.",
      "acceptanceCriteria": [
        "Hero on homepage with CTA",
        "npm run build exits 0"
      ],
      "priority": 4,
      "passes": true,
      "verify": "npm run build && grep -R -q 'Enter the comparison' src"
    },
    {
      "id": "US-005",
      "title": "Interactive scoreboard",
      "description": "Rows: open weights, closed flagships, agents, video, speech, robotics, on-device, export, energy, talent. Hover expands a note. Label estimates. Real lab names, not Acme. NEVER emit src/app/globals.css \u2014 it already builds. New scoreboard component + page.tsx import only. Keep Boot and Hero.",
      "acceptanceCriteria": [
        "Scoreboard on homepage",
        "npm run build exits 0"
      ],
      "priority": 5,
      "passes": true,
      "verify": "npm run build && grep -R -q -E 'open weights|Open weights|openWeights' src && grep -R -q -E 'DeepSeek|xAI|robotics' src"
    },
    {
      "id": "US-006",
      "title": "Scrubbable 2016-2026 timeline",
      "description": "Real beats (AlphaGo, GPT-3, ChatGPT, GPT-4, Sora, Qwen, DeepSeek R1, etc). Estimates labeled. Scrub or click through years. NEVER emit src/app/globals.css \u2014 it already builds. New timeline component + page.tsx import only. Keep Boot, Hero, Scoreboard.",
      "acceptanceCriteria": [
        "Timeline on homepage",
        "npm run build exits 0"
      ],
      "priority": 6,
      "passes": true,
      "verify": "npm run build && grep -R -q '2016' src && grep -R -q '2022' src && grep -R -q -E 'AlphaGo|GPT-3|DeepSeek' src"
    },
    {
      "id": "US-007",
      "title": "Lab dossiers",
      "description": "America: OpenAI, Anthropic, DeepMind, xAI, Meta + one dark horse. China: Qwen, DeepSeek, ByteDance, Zhipu, Moonshot, Huawei. Each dossier is a designed card in its rail, not a bullet list. NEVER emit src/app/globals.css. New dossier component + page.tsx import only. Keep prior sections.",
      "acceptanceCriteria": [
        "Dossiers on homepage",
        "npm run build exits 0"
      ],
      "priority": 7,
      "passes": true,
      "verify": "npm run build && grep -R -q 'OpenAI' src && grep -R -q 'Anthropic' src && grep -R -q 'Qwen' src && grep -R -q 'Moonshot' src && grep -R -q 'DeepSeek' src"
    },
    {
      "id": "US-008",
      "title": "Essay + toys + colophon",
      "description": "src/components/essay.tsx ALREADY EXISTS. Do not rewrite it. colophon and cmdk already exist in src. ONLY ONE FILE remains broken: src/app/page.tsx fails to compile. Emit ONLY src/app/page.tsx, nothing else. Hard rules for page.tsx: line 1 must be exactly \"use client\"; (with quotes and semicolon). ALL import statements go at the top of the file, lines 2-15, BEFORE any component code. NEVER place an import statement after a closing brace or at the bottom of the file \u2014 the current file has import { useState } from \"react\" on line 385 after the component, which breaks the build. Keep the existing structure: import Essay from \"../components/essay\" and keep Boot/Hero/Scoreboard/Timeline/Dossiers and the Colophon and CommandK usage. Do NOT emit arena.tsx, globals.css, essay.tsx, or any other file.",
      "acceptanceCriteria": [
        "Essay and at least two toys on homepage",
        "Colophon lists typefaces",
        "npm run build exits 0"
      ],
      "priority": 8,
      "passes": true,
      "verify": "npm run build && grep -q Essay src/app/page.tsx && grep -R -q -E 'colophon|Colophon' src && grep -R -q -E 'cmd-k|cmdk|CommandK|Meta\\+K' src"
    },
    {
      "id": "US-009",
      "title": "YouTube film polish",
      "description": "Final pass so the page is filmable at 1440px: spine glows, rails feel like two worlds, no Inter, no purple slop, substantial CSS, multiple components. If the homepage is still a heading and a paragraph, this story fails.",
      "acceptanceCriteria": [
        "At least 8 tsx files under src",
        "globals.css is at least 120 lines",
        "Dual-rail tokens still present",
        "npm run build exits 0"
      ],
      "priority": 9,
      "passes": false,
      "verify": "npm run build && test $(find src -name '*.tsx' | wc -l) -ge 8 && test $(wc -l < src/app/globals.css) -ge 120 && grep -E -q -- '--us-' src/app/globals.css && grep -E -q -- '--cn-' src/app/globals.css && grep -R -q 'Enter the comparison' src"
    }
  ]
}
